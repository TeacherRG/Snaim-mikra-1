export const APP_CONFIG = {
  // Путь к исходному тексту главы (текущий ассет в репозитории).
  textSource: './פרשת שלח _ שניים מקרא ואחד תרגום',

  // Путь к аудиофайлу озвучки.
  audioSource: './Record-6.mp3',

  // Длительность 1:04:00 (в секундах), если metadata недоступны.
  fallbackDurationSec: 64 * 60,

  // Скорости воспроизведения; массив легко расширяется новыми значениями.
  playbackRates: [1, 1.25, 1.5, 1.75, 2],

  // Опциональные ручные тайминги:
  // [{ word: 'וַיְדַבֵּר', start: 0.5, end: 1.2 }, ...]
  manualWordTimings: [],
};
